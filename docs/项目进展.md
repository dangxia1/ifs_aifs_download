# 项目进展

## 里程碑

### 数据源调研 (2026-04~05)
- 测试 ecmwf / azure / aws / google 四个数据源
- ecmwf 源仅保留 ~4 天，速度快，选为日常源
- azure 源历史数据完整，选为补缺和历史源
- 跨源一致性验证：ecmwf == azure（消息参数集合一致）
- GRIB 校验方案确定：eccodes 读取消息数 + 参数完整性

### 核心脚本 (2026-05)
- `dl_2026.py`：日常下载，ecmwf 源，每次最近 5 天
- `dl_anytime.py`：任意日期下载，azure 源，跳过已存在（由原 dl_2025.py + dl_miss.py 合并）
- `check_miss.py`：检查缺失文件
- 下载脚本自动创建目录，`setup.py` 非必须

### 项目规范化 (2026-05-06)
- git init 初始化仓库
- requirements.txt 版本锁定
- .gitignore 补充 Python 通用条目
- README.md 补充入门指引
- dl_anytime.py 使用 argparse

### 工程化 (2026-05-07~08)
- test/ 改为 pytest 自动化测试（conftest + 3 个 test 文件）
- print() 改为 logging 模块
- 配置从硬编码改为 config.yaml
- 添加 cron 定时运行指南（Linux/Win）
- 新增 check_miss.py 检查脚本
- steps 新增 step=0（分析场）
- 合并 dl_2025.py + dl_miss.py → dl_anytime.py
- 修复断点续传 bug：下载先写 .tmp，校验通过后原子更名，杜绝半文件残留
- 确认 AIFS 数据 2025-05-01 起可用（实测）

---

## 脚本指南

| 脚本 | 用途 | 命令 |
|---|---|---|
| `dl_2026.py` | 日常下载（ecmwf 源，ifs+aifs，最近5天） | `python scripts/dl_2026.py` |
| `dl_anytime.py` | 任意日期下载（azure 源，跳过已存在） | `python scripts/dl_anytime.py 2025-05-01 2025-08-31` |
| `check_miss.py` | 检查缺失（先看缺多少再决定补不补） | `python scripts/check_miss.py 2026-05-01 2026-05-07` |
| `setup.py` | 预建目录（首次用，下载时也会自动建） | `python scripts/setup.py 2026-05-01 2026-08-31` |

### 日常工作流

```bash
python scripts/dl_2026.py                              # 1. 每日拉取最近5天
python scripts/check_miss.py 2026-05-01 2026-05-07     # 2. 检查缺口
python scripts/dl_anytime.py 2026-05-01 2026-05-03     # 3. 有缺口就补
```

### 两个下载脚本的分工

| | dl_2026.py | dl_anytime.py |
|---|---|---|
| 数据源 | ecmwf（快，1-2 MB/s） | azure（慢，~200 KB/s） |
| 窗口 | 最近 5 天 | 任意日期 |
| 用途 | 每日增量 | 历史全量 / 补缺口 |
| 策略 | 过期后 404 自动跳过 | 下载所有缺失文件 |

### 配置

`scripts/config.yaml` — 可调参数：`times`、`steps`、`retry_max`、`retry_interval`、`save_dir`、`log_dir`。

### 测试

```bash
pytest test/ -v    # 需联网，验证 ecmwf / aifs / azure 三源下载正常
```

### 定时运行

见 [docs/cron.md](cron.md)（Linux crontab / systemd / Windows 任务计划）。

---

## 文件结构

```
data/raw/
├── 2025/ 或 2026/
│   ├── ifs/
│   │   ├── 0/  6/  12/  24/  48/  72/
│   └── aifs-single/
│       └── 同上
log/
└── {YYYYMM}/
    └── {YYYYMMDD}_download.log
```

每个 step 目录下：`{date}_{model}_t{time}_step{step}.grib2`，内含 40 条 GRIB 消息（4 单层 + 36 气压层）。

## 核心流程

```
遍历 model → date → time → step:
  target 存在 → SKIP（已完成）
  下载 surface + pressure levels → 两个临时文件
  二进制合并 → target.tmp（防止中断留半文件）
  eccodes 校验 40 条消息
    → OK: os.replace → target（原子更名）
    → FAIL: 删除 .tmp，重试
  404 → [EXPIRED] 不重试
  其他异常 → 清理 .tmp → 最多重试 3 次，间隔 10s
```

## 数据量估算

| 范围 | 文件数 | 数据量 | ecmwf 耗时 | azure 耗时 |
|---|---|---|---|---|
| dl_2026.py 每次 (2模型×5天×4时次×6步) | 240 | ~6 GB | ~1 小时 | — |
| 单月 (31天) | 1,488 | ~37 GB | — | ~35-60 小时 |
| 全年 (5~8月, 123天) | 5,904 | ~150 GB | — | **~6 天** |
| 建议预留 | — | 300 GB | — | — |

- ecmwf 源：~15s/文件（1-2 MB/s）
- azure 源：~70-300s/文件（80-300 KB/s），挂 VPN 更快
- 下载中断后重跑会自动跳过已完成文件，不丢进度