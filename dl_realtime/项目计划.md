### 网页展示服务器操作：
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false nohup streamlit run app.py --server.port 8501 --server.headless true > /shared_data/zongshen/ec_realtime/log/streamlit.log 2>&1 &

### 导师有新要求:

#### 1. 可视化图片使用中文（给当地气象局介绍）

- **现状**：图内文字已确认中文化范围：
  - 预报图：起报时间中文、色标说明（蓝/黄/橙/橙红/红）中文 ✓
  - 时序图：图例（1级~5级、达标未识别）中文 ✓
- **本次改动（2026-08-04）**：
  - **预报图标题** → 预报时间（起报北京时间 + step）：`IFS 2026/08/04 20:00`
  - **预报图右下角角标** `ARIA-Globe vn.17` → **删除**
  - **时序图标题** → `IFS | 起报 08/04 02:00 · 华北 IVT 强度演变 (0-144h)`
- **字体方案（最稳妥）**：项目内置 `fonts/` 目录放 Noto CJK 字体（OFL 开源可分发），代码优先从项目内加载，服务器/绿色包/本地三方一致，不依赖系统字体

#### 2. 网页右侧 step 改为具体时间

- **现状**：右侧按钮显示 `step0`/`step6`...（预报时长）
- **目标**：显示有效时间（起报北京时间 + step），如 `08/04 20:00`
- **对策**：
  - `dl_realtime.py` 运行时写入 `figures/run_time.json`：`{"run_time": "2026-08-04 14:00"}`（北京时间）
  - `app.py` 读取 json，每个按钮 = 起报 + step 小时 → `MM/DD HH:00`
  - 服务器和绿色包均自动适配（json 随图打包）

#### 3. 绿色包：直接运行代码并更新

- **用户想法 A**：服务器内网穿透（frp/ngrok）→ 任意电脑访问
  - 结论：**风险大，暂不采用**（服务器暴露公网有安全风险，稳定性依赖第三方服务）
- **用户想法 B（采纳）**：分两阶段
  - **阶段 1（现在）**：绿色**展示包**——只读模式，预生成图 + streamlit + 一键启动，发给别人演示
  - **阶段 2（气象局实际应用时）**：打包**完整代码包**——含下载/IVT/AR/可视化全套 + 阈值文件 + 依赖，对方电脑可自行更新数据
- **阶段 2 技术风险（提前评估）**：
  - `basemap`：Windows pip 可能无 wheel → 备选改用 `cartopy`（pip 有 Windows wheel）
  - `eccodes`：pip 有 Windows wheel（已本地验证）✓
  - `fil-finder/astropy/metpy/skimage`：pip 可装 ✓
  - 阈值文件（老师 12 个 pckl）：需打包进包
  - 首次运行：下载 1.3GB + AR 检测 30-40 分钟
  - 数据目录：config 的 save_dir 指向包内相对路径

### 图元素含义说明（2026-08-04 整理）

| 元素 | 含义 | 来源 |
|------|------|------|
| 图上红色加号 | **AR 质心**：每条识别出的大气河的中心位置 | `detect_ar.py` 按 IVT 加权计算每条河的质心，`visualize_ivt.py` 红色 `+` 标记 |
| 时序图 IVT 强度值 | 该 step 华北区域（35-45°N, 113-120°E）内 **IVT 最大值** | `north_china_timeseries.py`：`np.nanmax(IVT[mask])` |
| "达标未识别"（透明斜线柱） | 华北最大 IVT ≥ 250（达到 1 级门槛），但 AR 检测在该区域**未识别出**任何大气河羽流 | `level = -1`：`max_ivt >= 250 and not plume.any()` |
| 柱状图颜色 | AR 等级色：1级蓝 / 2级黄 / 3级橙 / 4级橙红 / 5级红（IVT 250-500 / 500-750 / 750-1000 / 1000-1250 / 1250+） | 导师要求分级 |

### 执行顺序
1. 图内文字改中文 + 标题改预报时间 + 去角标（visualize_ivt.py / north_china_timeseries.py）✅ 已完成
2. run_time.json + app.py 右侧按钮显示有效时间
3. 阶段 1 展示包更新（含中文图 + 时间按钮）
4. 阶段 2 完整包（等气象局实际应用时再做）