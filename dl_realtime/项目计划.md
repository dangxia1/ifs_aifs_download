  cd /home/zongshen/ifs_aifs_download && git pull
     
  # 安装新依赖
  conda activate ifs_aifs
  conda install -c conda-forge basemap basemap-data-hires -y
  pip install streamlit pillow

  # 首次跑全流程（25 step × 2 模型 = 50 文件，下载约 1.3GB + AR 检测）
  cd dl_realtime
  python dl_realtime.py

  # 启动展示界面
  streamlit run app.py --server.port 8501

  浏览器打开 http://服务器IP:8501。

  STREAMLIT_BROWSER_GATHER_USAGE_STATS=false nohup streamlit run app.py --server.port 8501 --server.headless true > /shared_data/zongshen/ec_realtime/log/streamlit.log 2>&1 &